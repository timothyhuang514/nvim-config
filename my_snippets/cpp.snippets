snippet bare "barebone code template"
	#include <iostream>
	#include <vector>
	#include <string>
	#include <map>
	#include <unordered_map>
	#include <set>
	#include <unordered_set>
	#include <stack>
	#include <queue>
	#include <numeric>

	using std::cout;
	using std::endl;
	using std::vector;
	using std::string;
	using std::map;
	using std::unordered_map;
	using std::set;
	using std::unordered_set;
	using std::stack;
	using std::queue;
	using std::pair;
	using std::make_pair;

	int main()
	{

		return 0;
	}

snippet icd "#include directive"
	#include <$1>
	$0

snippet plist "print vector"
	template <class T>
	void printList(const T& arr, const string& desc){
		std::cout << desc << ": [";

		for (auto it = arr.begin(); it != arr.end(); it++){
			std::cout << *it << ((std::next(it) != arr.end()) ? ", " : "");
		}
		std::cout << "]\n";
	}

snippet pmat "print list of list"
	template <class T>
	void printMat(const vector<vector<T>>& mat, const string& desc){
		cout << desc << ": " << endl;

		for (auto it1 = mat.begin(); it1 != mat.end(); it1++){
			auto cur_vec = *it1;
			cout << "[";
			for (auto it2 = cur_vec.begin(); it2 != cur_vec.end(); it2++){
				cout << *it2 << ((std::next(it2) != cur_vec.end()) ? ", " : "]\n");
			}
		}
	}

snippet pqueue "print queue"
	template <class T>
	void printQueue(T q){
		while(!q.empty()){
			std::cout << q.top() << " ";
			q.pop();
		}
		std::cout << '\n';
	}

snippet cout "print a variable"
	cout << "$1: " << $2 << endl;

snippet random "Generate a random list"
	// Generate a random sequence of length len, in range(low, high) (inclusive).
	// need to #include<random>
	vector<int> genRandom(int low, int high, int len){
		std::random_device rd;
		std::mt19937 gen(rd());
		std::uniform_int_distribution<int> distribution(low, high);

		vector<int> arr(len, 0);
		for (int i = 0; i != len; ++i){
			arr[i] = distribution(gen);
		}

		return arr;
	}

snippet incset "Use set"
	#include <set>

	using std::set;

snippet incmap "Use map"
	#include <map>

	using std::map;

snippet incqueue "Use queue"
	#include <queue>

	using std::queue;

snippet incstr "Use string"
	#include <string>

	using std::string;

snippet incvec "Use vector"
	#include <vector>

	using std::vector;

snippet incstack "Use stack"
	#include <stack>

	using std::stack;

snippet vec "std::vector"
	vector<$1> ${2:vec}

snippet map "std::map"
	map<$1, $2> ${3:mymap}

snippet umap "std::unordered_map"
	unordered_map<$1, $2> ${3:mymap}

snippet set "std::set"
	set<$1> ${2:myset}

snippet uset "std::unordered_set"
	unordered_set<$1> ${2:myset}

snippet queue "std::queue"
	queue<$1> ${2:q}

snippet stack "std::stack"
	stack<$1> ${2:mystack}

snippet sol "solution"
	auto solution = Solution();
	$0

snippet for "for loop"
	for ($1; $2; $3){
		$4
	}

snippet if "if condition"
	if ($1){
		$2
	}
	$0

snippet ifelse "if else condition"
	if ($1){
		$2
	}else{

	}
